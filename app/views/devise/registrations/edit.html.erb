<div class="main-content">
  <div class="container">
    <%= form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :method => :put, :role => 'form'}) do |f| %>
    <div class="row">
      <div class="col-md-12">
        <h3>Configuración de la cuenta</h3>
        <%= devise_error_messages! %>
      </div>
    </div>

    <div class="row">
      <div class="col-md-3">
        <h4>Datos personales</h4>
        <div class="form-group">
          <%= f.label :first_name, "Nombre" %>
          <%= f.text_field :first_name, :autofocus => true, class: 'form-control' %>
        </div>
        <div class="form-group">
          <%= f.label :last_name, "Apellidos" %>
          <%= f.text_field :last_name, class: 'form-control' %>
        </div>
        <div class="form-group">
          <%= f.label :email, "Correo electrónico" %>
          <%= f.email_field :email, class: 'form-control' %>
          <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
            <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
          <% end %>
        </div>
      </div>

      <div class="col-md-3 col-md-offset-1">
        <h4>Cambio de contraseña</h4>
        <p>Si no quieres cambiar tu contraseña deja estos espacios en blanco</p>
        <div class="form-group">
          <%= f.label :password, "Contraseña" %>
          <%= f.password_field :password, :autocomplete => 'off', class: 'form-control' %>
        </div>
        <div class="form-group">
          <%= f.label :password_confirmation, "Confirmación de la contraseña" %>
          <%= f.password_field :password_confirmation, class: 'form-control' %>
        </div>
      </div>
    </div>

    <div class="row">
      <div class="col-md-3">
        <hr>
        <p>Introduzca su contraseña actual para realizar cambios</p>
        <div class="form-group">
          <%= f.label :current_password, "Contraseña actual" %>
          <%= f.password_field :current_password, class: 'form-control' %>
        </div>
        <%= f.submit 'Actualizar datos', :class => 'ok-button' %>
      </div>
    </div>

    <div class="row">
      <div class="col-md-12">
        <hr>
      </div>
    </div>
    <% end %>
    
    <div class="row">
      <div class="col-md-6">
        <h3>Cancelación de la cuenta</h3>
        <p>Si no estas satisfecho con EdificaPRO puedes cancelar tu cuenta de usuario/a.</p>
        <p>Recuerda que los cursos son comprados, por lo tanto puedes acceder a ellos sin tener que pagar una cuota mensual. Si cancelas tu cuenta perderás la posibilidad de acceder a tus cursos.</p>

        <%= button_to "Cancelar mi cuenta de usuario/a", registration_path(resource_name), :data => { :confirm => "Estás cancelando tu cuenta de usuario/a. ¿Seguro que quieres continuar?" }, :method => :delete, :class => 'danger-button' %>
      </div>
    </div>
  </div>
</div>
